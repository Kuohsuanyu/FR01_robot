# Q-BOT training requirements (Linux + NVIDIA GPU recommended)
# Python >= 3.11

ksim==0.1.3
xax==0.3.0
mujoco-scenes
mujoco>=3.2

# JAX with CUDA (GPU). On a Linux GPU box run:
#   pip install -r requirements.txt
#   pip install "jax[cuda12]"
# CPU-only fallback (very slow, for smoke tests only):
#   pip install "jax[cpu]"
